
server {

  listen 80;
  server_name localhost;
  charset utf-8;

  location ^~ /static/ {
    root {{ osp_www_src }};
  }

  location / {
    try_files $uri @osp;
  }

  location @osp {
    include uwsgi_params;
    uwsgi_pass 127.0.0.1:4242;
  }

}
